# Arithmetic Dynamics — Notes

## Navigation

- Added a persistent "🏠 Home" link fixed to the top-left corner of the
  game page, linking to `/` so players can return to the site index at
  any time without disrupting the level bar or game controls.
- Styled via `.home-link` in `styles/main.css`, matching the game's
  neon/panel aesthetic (accent2 border, panel-solid background).

## Follow-ups

- If a shared site-wide header/nav component is introduced later,
  this per-game home link can be replaced or reused as its "back to
  hub" element.
